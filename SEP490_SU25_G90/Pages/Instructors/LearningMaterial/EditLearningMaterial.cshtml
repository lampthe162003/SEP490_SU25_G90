@page
@model SEP490_SU25_G90.Pages.Instructors.LearningMaterial.EditLearningMaterialModel

@{
    ViewData["Title"] = "EditLearningMaterial";
    Layout = "~/Pages/Shared/_InstructorLayout.cshtml";
}

<h1>Chỉnh sửa Tài liệu học</h1>

<form method="post" enctype="multipart/form-data">
    <input type="hidden" asp-for="Input.MaterialId" />
    <input type="hidden" asp-for="Input.OldFilePath" />

    <div class="mb-3">
        <label asp-for="Input.Title" class="form-label"></label>
        <input asp-for="Input.Title" class="form-control" />
        <span asp-validation-for="Input.Title" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Input.Description" class="form-label"></label>
        <textarea asp-for="Input.Description" class="form-control"></textarea>
        <span asp-validation-for="Input.Description" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Input.LicenceTypeId" class="form-label">Loại bằng</label>
        <select asp-for="Input.LicenceTypeId" class="form-select" asp-items="Model.LicenceTypeOptions">
            <option value="">-- Chọn loại bằng --</option>
        </select>
        <span asp-validation-for="Input.LicenceTypeId" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="Input.File" class="form-label">Tệp PDF mới (nếu thay đổi)</label>
        <input asp-for="Input.File" type="file" class="form-control" />
        <span asp-validation-for="Input.File" class="text-danger"></span>

        @if (!string.IsNullOrEmpty(Model.Input.OldFilePath))
        {
            <div class="mt-2">
                <strong>File hiện tại:</strong>
                <a href="@Model.Input.OldFilePath" target="_blank">
                    @System.IO.Path.GetFileName(Model.Input.OldFilePath)
                </a>
            </div>
        }
    </div>

    <button type="submit" class="btn btn-primary">Lưu</button>
    <a asp-page="/Instructors/LearningMaterial/LearningMaterialList" class="btn btn-secondary">Quay lại</a>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
